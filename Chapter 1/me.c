#include<stdio.h>


int main()
{
    // **Integer = Numbers 1,2,3...**
    // IMPORATANT : use ("%d") for integer
    // Example : 
      int a = 6;
                
                return 0;



    // **float = Decimals 1.5, 1.6, 1.7...**
    // IMPORATANT : use ("%f") for float
    float b = 2.4;
    
     return 0;


    // **char = character means a, @, $**
    // IMPORTANT : use single quote('') to provide value to a character
    // IMPORATANT : use ("%c") for character
    char c = 'z'
    printf("%c", c);
    printf("%f", b);
    printf("%d", a);
                return 0;
}

// IMPORATANT : use ("/n") to get a new line
    // printf("The value of a is %d\n", a);
    // printf("The value of b is %f\n", b);
    // printf("The value of c is %c\n", c);